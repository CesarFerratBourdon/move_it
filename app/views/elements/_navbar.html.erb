<nav class="navbar navbar-default navbar-header navbar-fixed-top container centered">

  <%= link_to (image_tag('MoveIT.png', width: "200px")), defined?(root_path) ? root_path : "#"%>

  <div class="navbar-right">
  <% if defined?(Devise) %>
    <% if user_signed_in? %>
      <ul class="dropdown">
        <button type="button" class="btn btn-default navbar-btn dropdown-toggle" data-toggle="dropdown"><strong>Account<b class="caret"></b></strong></button>
        <ul class="dropdown-menu">
          <li><%= link_to "Edit Account", edit_user_registration_path %></li>
          <li class="divider"></li>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
        </ul>
      </ul>
    <% else %>
    <button type="button" class="btn btn-default navbar-btn"><strong><%= link_to "Login", new_user_session_path %></strong></button>
    <button type="button" class="btn btn-default navbar-btn"><strong><%= link_to "Sign Up", new_user_registration_path %></strong></button>
    <% end %>
  <% end %>

</div>
</nav>
